# Primitive bash build system

# Build JSX -> JS
jsx < notes.jsx > bundle.js

# Echo
echo Done building targets.
echo
echo Switch to minified thirdparty JS.
echo Remove browser.min.js
echo Switch to bundle.js